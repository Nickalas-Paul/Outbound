# Expo loads env from apps/universal/ (copy to .env and set your token)
EXPO_PUBLIC_MAPBOX_TOKEN=your-mapbox-token-here

# API base URL. Use LAN IP for physical-device / native testing.
# Web can use localhost or the same LAN IP.
EXPO_PUBLIC_API_URL=http://localhost:3001
EXPO_PUBLIC_APP_URL=http://localhost:8081

# Optional: if EXPO_PUBLIC_API_URL is localhost, native will fall back to this.
# EXPO_PUBLIC_API_URL_LAN=http://192.168.1.139:3001

# Feature gating (Phase 6). Shared with API; false = all unlocked (beta).
OUTBOUND_GATING_ENABLED=false
# Expo client (must be EXPO_PUBLIC_ to reach the bundle):
EXPO_PUBLIC_OUTBOUND_GATING_ENABLED=false

# Cloudflare Turnstile site key (client widget).
# Test keys for development; real keys in Phase 5.
# Site: 1x00000000000000000000AA  Secret: 1x0000000000000000000000000000000AA
EXPO_PUBLIC_TURNSTILE_SITE_KEY=1x00000000000000000000AA
# Optional override for native WebView page (defaults to ${API}/api/intake/turnstile)
# EXPO_PUBLIC_TURNSTILE_PAGE_URL=http://localhost:3001/api/intake/turnstile

# Google Sign-In (native) — web OAuth client ID
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your-google-client-id
